{
  "name": "remote-server",
  "version": "1.0.0",
  "description": "Remote Socket IO based server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gener ZV",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.0",
    "socket.io": "^2.0.3"
  }
}
